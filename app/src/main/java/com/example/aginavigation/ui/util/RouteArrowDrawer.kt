package com.example.aginavigation.ui.util

import com.google.android.gms.maps.GoogleMap
import com.google.android.gms.maps.model.LatLng
import com.google.android.gms.maps.model.Polyline

class RouteArrowDrawer(private val map: GoogleMap) {
    // Placeholder implementation
    fun drawArrows(polyline: Polyline) {
        // Implementation to draw arrows along the polyline
    }
    
    fun drawArrows(points: List<LatLng>) {
         // Implementation to draw arrows along the points
    }
}
